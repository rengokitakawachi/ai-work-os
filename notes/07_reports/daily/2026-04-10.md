# Daily Report - 2026-04-10

## 今日の成果

- `web 版 GPT editor で ADAM instruction の最新変更を反映する` を完了した
- review / operations の日中運用原則を instruction と review spec に反映した
- `operations → Todoist projection` に
  - 事前参照
  - date / deadline 変換
  - 同名 task 許容
  - `todoist_task_id` 優先更新
  を実装した
- Todoist 無料プラン中は deadline を skip して継続する fallback を design / code に反映した
- `active_operations` の task を EVE開発 project に再作成し、operations 正本との `todoist_task_id` 対応を揃えた
- `active_operations` の全 task に `due_date / due_type: date` を追加し、Todoist 側へ日付反映を完了した

## 重要な意思決定

- operation の rolling は原則 daily review でのみ実行する
- archive への移動は daily review で固定する
- 会話中は active 内の優先順位変更や並び替えまでは許可する
- Todoist projection では
  - date は反映する
  - deadline は無料プラン中は skip する
  - 上書き対象の主識別子は `external.todoist_task_id` とする
- Todoist task を変更する前に、対象 project の現状 task を先に参照する

## 学び / 気づき

- 同名 task を許容しても、`todoist_task_id` を正本側に保持すれば安全に update できる
- due_date を task ごとに明示すると、Todoist への date 反映は安定する
- deadline は schema 上は保持しつつ、実行時のプラン制約で skip する設計が現実運用に合う

## 未解決 / リスク

- Day 見出し日付を自動で読み取って Todoist date に落とす運用は未接続
- Todoist 有料化後の deadline 実反映は未再確認
- projection 実行結果を daily report / handover にどう定型返却するかはまだ固まっていない

## 次のアクション

- `Phase 0 plan に対する現行 operations の接続を見直す` から再開する
- weekly review 前提で active / next / archive の更新準備に進む
- execution governance の docs 反映候補整理に着手する

## 明日の再開点

- Day0 の `Phase 0 plan に対する現行 operations の接続を見直す` から始める
- operations と Phase 0 plan の対応を読み直し、現行 active / next が完了条件にどう効くかを整理する

## 総評

4/10 は、review 原則・operations 正本・Todoist execution view の境界を実運用レベルで揃えた日だった。
特に、daily review 固定の reroll 原則、無料プラン前提の deadline fallback、active_operations と Todoist task の再同期まで通せたことで、明日からは実行順そのものの見直しに集中できる状態になった。
