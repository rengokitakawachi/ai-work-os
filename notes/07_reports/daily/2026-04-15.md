# Daily Report - 2026-04-15

## 今日の成果

- `notes/02_design/2026-04-15_issue_routing_variable_assessment_schema.md` を作成し、issue routing の可変評価 schema を design として固定した
- `src/services/flow-control/rules.js` に `evaluated_at / impact_now / urgency_now` を追加し、routing decision の可変評価項目を返せるようにした
- `src/services/flow-control/adapters.js` に `default_impact / default_urgency` を追加し、input 側を default assessment 前提へ寄せた
- `src/services/flow-control/issue-routing-actions.js` に可変評価項目を通し、action plan 側でも判断文脈を保持できるようにした
- 関係コードを読み直し、`placement.js` と `normalize.js` で値が落ちている問題を発見した
- `src/services/flow-control/placement.js` と `src/services/flow-control/normalize.js` を修正し、可変評価 schema が end-to-end で流れる経路を補正した
- daily review を実行し、active_operations を翌日基準へ rolling した
- Todoist projection の drift を確認し、前回完了済みの task を close した
- 現在の Day0 task を Todoist に新規作成し、`active_operations.md` へ external.todoist_task_id を反映した

## 重要な意思決定

- issue routing の可変評価は、issue 本体ではなく default assessment と routing decision に分けて扱う
- schema を notes write より先に固定し、返り値の安定を優先する
- Todoist は operations の projection であり、daily review では projection 同期まで閉じる
- 可変評価 schema の確認では、構文だけでなく placement / normalize を含む値の通り道を必ず再点検する

## 学び / 気づき

- design と code を別々に前進させても、placement 層で値が落ちると end-to-end では成立しない
- schema 拡張時は、input / normalize / rules / placement / routing / action plan を通して見る必要がある
- projection drift は review の最後で見ないと残りやすい
- Day0 task が Todoist に未投影でも operations 側では先頭 task として存在しうるため、projection 同期の確認は独立手順として必要である

## 未解決 / リスク

- issue routing の action plan を notes write へ接続する usecase は未実装
- 1ファイル dry run で可変評価項目が実際に routed_candidates / action plan に乗るかの再確認は未了
- Todoist projection は current active に対して同期したが、next / archive との同期方針はまだ粗い
- review 出力から content 抽出と rolling をどう安定接続するかの design は未着手

## 次のアクション

- `issue routing の後処理統合と可変評価 schema の整理を進める`
- 1ファイル dry run で `evaluated_at / impact_now / urgency_now` が end-to-end で流れるかを確認する
- action plan を notes write へ接続する最小 usecase を検討する

## 明日の再開点

- Day0 の `issue routing の後処理統合と可変評価 schema の整理を進める` から再開する
- まず dry run で評価文脈が routed_candidates と action plan に残っているか確認する
- 問題なければ notes write 接続の最小案へ進む

## 総評

2026-04-15 は、issue routing の可変評価 schema を design だけでなく code の通り道全体に広げた日だった。途中で placement と normalize に穴が見つかったが、関係コードを読み直して flow 全体の欠落を補正できたことで、Day0 の論点は一段締まった。次は、設計した schema が実データ dry run でも崩れないことを確かめたうえで、後処理統合を notes write に接続する段階へ進む。