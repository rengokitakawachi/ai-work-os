# Daily Report - 2026-04-18

## 今日の成果

- `repo-resource` の code allowlist を拡張し、`package.json` / `vitest.config.js` / `jest.config.js` など repo root config file を code resource から read / create / update 可能にした
- `package.json` の read と code tree 反映まで確認し、repo root allowlist 変更要求 task を完了扱いにした
- `flow-control` の `rules.js` / `ranking.js` に最小回帰テストを追加した
- `api/repo-resource.js`、`api/tasks/index.js`、`api/tasks/[id].js`、`api/tasks/project.js` に最小 handler テストを追加した
- `src/services/tasks/projection.js` の純関数部分に対する最小テストを追加した
- `package.json` に `node --test` ベースの `test` script を追加し、最小テスト実行入口を整えた
- 一時再構成での実行確認を進め、少なくとも追加した最小テスト群の大半が現行 code と整合することを確認した
- `src/services/todoist.js` を deprecated 候補として issue / design / code comment に反映した
- 過去の handover / design / dev_memo を横断し、`src/services/todoist.js` が「昔の中心実装から `client.js` 正本へ移った legacy 候補」である根拠を整理した
- 2026-04-18 daily review として、完了 task を archive へ移し、active / next / Todoist projection を 2026-04-19 基準へ更新した

## 重要な意思決定

- repo root の code access は全開放ではなく allowlist 方式で広げる
- 最小テスト導入の初手は pure / handler の分岐ロジックから固める
- `src/services/todoist.js` は即削除せず、deprecated → usage 最終確認 → 削除の順で扱う
- `src/services/todoist.js` の legacy 判定は current code だけでなく、履歴文脈でも裏付けてから進める
- 2026-04-18 daily review では、完了済み Day0/Day3-6 task を archive へ移し、未完了と次候補を 2026-04-19 以降へ rolling する

## 学び / 気づき

- `node:test` は既存実装に小さく載せやすく、pure function と handler validation を先に押さえるだけでも回帰防止ラインとして十分価値がある
- legacy code の廃止判断は、現在未使用に見えることだけでなく、「なぜ残っているのか」という履歴を掘ると説明力が上がる
- `repo-resource` の root allowlist を持たせると、package.json や test runner 設定の確認精度が大きく上がる
- daily review は report 保存では終わらず、active / next / archive と Todoist projection を閉じて初めて完了になる

## 未解決 / リスク

- `design-routing.test.js` と reroll sample dry_run の repo 実体実行はまだ未了で、パソコン利用時確認が残っている
- `src/services/todoist.js` の repo 全体 hidden import はまだ最終確認が必要であり、削除そのものは未実施
- tasks handler テストの repo 実体フル実行は、ローカル実環境での最終再確認があるとより確実である

## 次のアクション

- `design routing test の shape / import 崩れを静的に洗う`
- `パソコンで design routing test と reroll sample dry_run を実行する`
- `src/services/todoist.js の repo 全体 usage を最終確認する`
- `applyDesignRoutingActionPlan の skeleton を切る`

## 明日の再開点

- Day0 先頭の `design routing test の shape / import 崩れを静的に洗う` から再開する
- その後、パソコン環境で `design-routing.test.js` と reroll sample dry_run の実行確認へ進む
- PC 実行確認のあと、`src/services/todoist.js` の hidden import 最終確認を閉じる

## 総評

2026-04-18 は、repo resource の root access 改善、最小テスト導入、legacy Todoist wrapper の deprecation 判断整理が同時に前進した日だった。単にコードを足すだけでなく、テスト入口、履歴根拠、operations / Todoist projection までつないで daily review を閉じられた点が大きい。一方で、design routing の repo 実体実行確認と `src/services/todoist.js` の最終 usage 確認はまだ残っているため、次はそこを Day0 本線として詰めるのが自然である。
