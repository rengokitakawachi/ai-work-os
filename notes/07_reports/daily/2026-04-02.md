# Daily Report - 2026-04-02

## 今日の成果

- notes の indexed 構造を継続整理し、`00_inbox` 直下および `00_inbox/web` の実体移設を大きく前進させた
- `00_inbox` に未移設だった記事・メモ群を多数複写し、旧 `inbox` との実ファイル照合ができる状態まで整理した
- `01_issues` を新規作成し、`xx_ideas/idea_log.md` を `01_issues/idea_log.md` として正式受け先に昇格させた
- `03_plan/README.md` を作成し、既存 notes の定義に合わせて `phase → plan → operations` の役割分離を反映した
- 旧新フォルダの一致確認を実施し、README を無視した実データ一致ベースで削除可否を判定した
- `design / operations / analysis / content / decisions / logs / handover / reports / xx_ideas` が実質一致であることを確認した
- `inbox ↔ 00_inbox` のズレを 4 本まで絞り込み、残件を明確化した
- 旧 `logs` を削除し、旧 `design` 配下の全ファイルを削除した

## 重要な意思決定

- `03` は `plan` とし、役割は「一定期間で進める重点テーマ」とする
- `plan` は notes、`phase` は docs、`operations` は notes として扱う
- フォルダ一致判定は README の有無を除外し、実ファイル本体の一致で見る
- 削除可否は「実データ一致」と「ツールの delete 制約」を分けて判断する
- 旧フォルダのうち `design / operations / analysis / content / decisions / logs / handover / reports / xx_ideas` は削除対象として扱える
- ただし ADAM が Action で削除できるのは `design` と `logs` に限る

## 学び / 気づき

- フォルダ移行は「名前の移行」より「実ファイル SHA の一致確認」が本質
- README の追加は構造説明として有効だが、移行完了判定とは切り分けて扱う必要がある
- `plan` のような中間レイヤーは推測で置くとぶれるため、既存 notes の記述に必ず戻る必要がある
- delete 制約の存在により、「削除可能」と「ADAM が削除実行可能」は別概念として管理した方が安全

## 未解決 / リスク

- `inbox ↔ 00_inbox` はまだ 4 本の残件がある
- そのうち 1 本は特殊ファイル名で Action から read できず、未移設のまま
- `Antigravity`、`SIGNATE web`、`Obsidian in Cursor` は SHA 不一致が残っており、不可視差分の可能性がある
- `handover / reports / content / decisions / operations / analysis / xx_ideas` は削除対象だが、Action の delete 制約により ADAM からは削除できない

## 次のアクション

- ユーザー側で `inbox` 残件 4 本を対応する
- その後 `notes` 整理完了として扱い、`docs/15_notes_system.md` の更新に着手する
- 続けて `notes/README.md` と必要な docs の整合確認を進める
- 最後に handover の next action へ戻る
