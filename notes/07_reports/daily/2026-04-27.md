# Daily Report 2026-04-27

## review mode

daily review

## summary

今日は、delta 開発へ進む前の前提として、main 整合回復と branch foundation を前進させた。

最大の進捗は、`feature/atlas-pre-delta-foundation` branch を ADAM runtime から作成し、read-back まで確認したこと。

一方で、ATLAS workflow 実装は `.nvmrc` / `.github/workflows/test.yml` が code resource write allowlist 外であることが判明し、次回の Immediate Gate として整理した。

---

## completed

- 現 main の docs/code 不一致を分類し、整合修正対象を確定した
- main 整合修正案を作成した
- feature branch target を `feature/atlas-pre-delta-foundation` に固定した
- docs/10 repoResource branch selector reflection の反映確認を完了した
- repoResource branch create API の actual behavior を確認した
  - `repoResourceWrite resource=repo action=create_branch branch=feature/atlas-pre-delta-foundation from_branch=main`
  - response: `status=CREATED`
  - source_sha: `33bfb564142efe8ba2feb114980911e874a630d7`
  - `package.json` read-back on `feature/atlas-pre-delta-foundation` succeeded

---

## blocked / carryover

- `ATLAS test workflow を feature branch へ実装する` は未完了
- blocker:
  - code resource write allowlist が `.nvmrc` と `.github/workflows/` を許可していない
  - `.nvmrc` create attempt failed with `INVALID_REQUEST: code path not allowed`
- next Immediate Gate:
  - `code resource write allowlist に ATLAS workflow 用 root / workflow path を追加する`

---

## operations update

- `active_operations.md` を更新した
  - 完了済み Day0〜Day4 を archive 対象へ移動
  - ATLAS workflow blocker を Immediate Gate に追加
  - Day0 を `ATLAS test workflow を feature branch へ実装する` に設定
  - Day1 以降を bulk / docs reflection / delta resource / delta planning へ reroll
- `next_operations.md` を更新した
  - active に入ったものと next に残すものを再整理
- `archive_operations.md` に 2026-04-27 daily review セクションを追加した

---

## Todoist projection

- `projectTasks(mode=apply)` は一部反映後、古い外部 task id に対して upstream 404 で停止した
- open tasks を確認し、以下を手動復旧した
  - Immediate Gate task を新規作成
  - docs/10 branch create reflection task は projection により作成済みであることを確認
  - delta initial roadmap / log template / Phase 0棚卸し task を新規作成
  - 古い非active projection task 2件を close
- 新規 Todoist task id を `active_operations.md` へ書き戻した

---

## next action

次回の最初の実行対象は Immediate Gate。

```text
code resource write allowlist に ATLAS workflow 用 root / workflow path を追加する
```

この gate が完了するまで、`ATLAS test workflow を feature branch へ実装する` 以降は実行しない。

---

## risk

- `projectTasks(mode=apply)` が古い external task id に弱く、projection が途中停止した
- Todoist projection は手動復旧済みだが、今後は stale external id がある場合の扱いを改善する余地がある
- code resource allowlist の拡張は、workflow 実装に必要な最小範囲に限定する必要がある
