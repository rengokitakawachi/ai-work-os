# Daily Report - 2026-04-20

## 今日の成果

- issue routing の完成条件を、実装完了ではなく「実運用で効果が見えること」として Phase 0 plan / operations に反映した
- issue routing の最小運用実験で使う issue 候補と観測項目を固定し、`20260418-022` と `20260419-023` を第一バッチ入力に定めた
- `routing と document writing の責務分離方針` を design に整理し、routing は action plan 生成まで、writer は payload 生成以降という境界を固定した
- `issue routing と writer 間の action plan 受け渡し項目` を design に整理し、`normalized_items / routing_decisions / action_plan` の3層を固定した
- issue routing では、送付先がまだ自然でなければ無理に振り分けず issue に残してよいことを design に明示した
- repo 上の `config/ai/adam_instruction.md` には再発防止ルールを反映した
- daily review として、完了 2 件を archive へ移し、未完了 task を 2026-04-21 基準へ rolling し、Todoist projection も更新した

## 重要な意思決定

- issue routing は「必ずどこかへ送る処理」ではなく、`route_to: issue` / `keep_items` を正規結果として扱う
- routing と document writing は分離し、completed document ではなく action plan で引き渡す
- repo の `adam_instruction.md` 更新と、実際の ADAM instruction 反映は別物として扱う
- operations 提案 / reroll では、前提変更チェック・Day 容量チェック・依存順チェックを instruction レベルでも持たせる

## 学び / 気づき

- routing の責務を軽くしないまま運用実験に進むと、観測対象が旧構造のままになりやすい
- issue routing の keep を正規結果にすると、無理な振り分けを避けつつ運用上の自然さを保てる
- repo 更新済みと実反映済みを混同すると、完了判定を誤りやすい
- Day 容量ルールは、個々の task の妥当性ではなく日単位の容量チェックまで行って初めて守れる

## 未解決 / リスク

- `repo の adam_instruction.md 変更を ADAM instruction へ反映する` は未完了であり、実運用中の instruction とのズレが残っている
- `routing と writing を分離する最小実装を入れる` は未着手であり、第一バッチ実験前の code 変更がまだ必要である
- issue routing 第一バッチ実験はまだ未実施で、`route_to: issue` を含む実運用観測はこれからである
- docs 番号衝突整理と Phase 1 接続案は後ろ倒しになっており、repo 整合と次 phase 接続はまだ残っている

## 次のアクション

- `repo の adam_instruction.md 変更を ADAM instruction へ反映する`
- `routing と writing を分離する最小実装を入れる`
- `issue routing の第一バッチ運用実験を実施する`
- `issue routing 第一バッチの観測結果を記録する`

## 明日の再開点

- Day0 の `repo の adam_instruction.md 変更を ADAM instruction へ反映する` から再開する
- 続けて `routing と writing を分離する最小実装を入れる` に進む
- その後、第一バッチ実験と観測記録へ進む

## 総評

2026-04-20 は、issue routing を「無理に振り分ける処理」から「必要なら issue に残せる処理」へ整理し直せた日だった。routing と writer の責務分離、action plan handoff schema、keep の正規化まで進んだため、次は code 側の最小実装と実運用実験へ進むのが自然である。
