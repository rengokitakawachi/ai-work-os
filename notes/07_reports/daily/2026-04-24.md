# Daily Report - 2026-04-24

## 今日の成果

- `pending_tasks` split 実装後の再観測を行い、route / source_ref / role boundary は維持され、1テーマ1メモ性は改善したと整理した
- 再観測で見つかった未達を design に返し、`概要` / `まとめ` / `summary` を導入 / メタ section として除外する最小ルールを固定した
- `src/services/flow-control/adapters.js` の `isExcludedPendingTasksSection()` に `概要` を追加し、導入ノイズの最小除外を実装した
- 実装後の軽い再確認を行い、導入 / メタ section が item 化される未達は閉じたと判断した
- `pending_tasks` split 後の inbox 後処理について、原則 archive、未判断が残るときのみ pending とする最小ルールを design に固定した
- Todoist に due のない task が出た原因を切り分け、`projectTasks` の runtime projection schema に `due_date` / `due_type` がないことが根本原因だと整理した
- due 欠落していた Todoist task 3 件に対して手動補正を行い、issue / design へ再発防止論点を保存した
- daily review として、candidate source を plan / open issue / next_operations / current active で確認し、完了 task の archive 移動、active / next の reroll、Todoist projection 更新方針まで整理した

## 重要な意思決定

- `pending_tasks` split の最小未達は導入 / メタ section 除外であり、`概要` を除外対象に追加するだけで閉じると判断した
- `pending_tasks` split 後の元 inbox file は、原則 archive、未判断が残る場合だけ pending とする
- routing 後の inbox は無理由で残さない
- Todoist due 欠落の根本原因は Todoist 側ではなく、repo projection 実装と runtime projection schema の不一致である
- `projectTasks` schema に `due_date` / `due_type` を通す対策を次の active 候補に入れる

## 学び / 気づき

- route correctness と粒度改善を分けて進めたことで、`pending_tasks` 論点を小さく安全に閉じられた
- 粒度改善が済んだ後に残る論点は parser ではなく inbox 後処理であり、completed condition の層を分けて見ることが重要だった
- source_ref が成立していれば、元 inbox を習慣的に残し続ける理由は弱い
- Todoist due 問題は notes 側の欠落ではなく、runtime projection interface の欠落だった

## 未解決 / リスク

- `daily review reroll` の candidate source 明示チェックはまだ design と runtime confirmation の両方が残っている
- Todoist due_date / due_type 伝播欠落の根治には `projectTasks` schema 側の拡張が必要
- `pending_tasks` 元 inbox を実運用上 archive 扱いへ寄せてよいかの runtime 確認は未実施
- intake routing の inbox 後処理 rule を一般化する論点は next_operations 側に残っている

## 次のアクション

- `daily review reroll で plan / issue / next を必須候補源として確認する最小チェックを design に落とす`
- `ADAM の instruction へ daily review reroll gate 反映を確認する`
- `flow-control 新 handoff shape 統一の到達点を weekly report に返す要点を整理する`
- `docs/05_roadmap.md に Phase 0 をどう位置づけるか整理する`
- `issue routing の完成条件を plan / operations に反映する`
- `legacy な Todoist service wrapper を deprecated 化する段取りを design / operations に落とす`
- `operations 提案時の Day 容量と reroll completeness を同時に確認するチェック項目を整理する`
- `Todoist projection の due_date / due_type 伝播欠落を埋める最小対策を design に落とす`
- `直近 issue を plan / operations / dev_memo へどう落とすかの運用を整える`

## 明日の再開点

- Day0 の `daily review reroll で plan / issue / next を必須候補源として確認する最小チェックを design に落とす` から再開する
- 同じ Day0 で `ADAM の instruction へ daily review reroll gate 反映を確認する` と `flow-control 新 handoff shape 統一の到達点を weekly report に返す要点を整理する` を進める
- 新スレッドなら冒頭に `ADAM_0425` を置いて再開する

## Todoist projection 更新結果

- close:
  - `pending_tasks 分解後の第一バッチ再観測を行う`
  - `pending_tasks 型 split 後の inbox archive / pending rule を整理する`
- keep:
  - `daily review reroll で plan / issue / next を必須候補源として確認する最小チェックを design に落とす`
  - `ADAM の instruction へ daily review reroll gate 反映を確認する`
  - `flow-control 新 handoff shape 統一の到達点を weekly report に返す要点を整理する`
  - `docs/05_roadmap.md に Phase 0 をどう位置づけるか整理する`
  - `issue routing の完成条件を plan / operations に反映する`
  - `legacy な Todoist service wrapper を deprecated 化する段取りを design / operations に落とす`
  - `operations 提案時の Day 容量と reroll completeness を同時に確認するチェック項目を整理する`
- create:
  - `Todoist projection の due_date / due_type 伝播欠落を埋める最小対策を design に落とす`
  - `直近 issue を plan / operations / dev_memo へどう落とすかの運用を整える`

## 総評

2026-04-24 は、`pending_tasks` 系の論点を「再観測 → 導入除外 design → 実装 → 軽い再確認 → archive / pending rule 固定」まで一段閉じられた日だった。特に、route correctness と粒度改善を分けたまま、最後に inbox 後処理 rule へ自然に接続できたのが大きい。また、Todoist due 欠落問題も notes 側ではなく runtime projection schema の不一致だと切り分けられたため、次の active に具体的な対策 task を置ける状態になった。daily review としては、candidate source を plan / open issue / next_operations / current active で確認し、完了 task の archive 移動、active / next の再配置、Todoist projection 更新方針まで進められたので、明日は reroll gate design から素直に再開できる。
